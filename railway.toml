[build]
builder = "NIXPACKS"
buildCommand = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && arq app.workers.pipeline.WorkerSettings & cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
